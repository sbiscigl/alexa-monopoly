language: java
jdk:
- openjdk8
after_success:
- mvn clean test jacoco:report coveralls:report
- mvn package
addons:
  artifacts:
    paths:
      - target/alexa-monopoly-1.0-SNAPSHOT.jar
    target_paths:
      - build/
